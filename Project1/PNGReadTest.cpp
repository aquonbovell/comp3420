// PNGTest.cpp : This file contains the 'main' function. Program execution begins and ends there.

/* #pragma warning(disable : 4996)
#pragma comment(lib, "shell32.lib")

#include <iostream>
#include "pngwriter.h"

int main() {
	// ==================== 1 ====================
	// This code snippet creates a mirror image of the original image
	//int height, width, red, blue, green;
	//pngwriter* source = new pngwriter();
	//source->readfromfile("sc.png");
	//width = source->getwidth();
	//height = source->getheight();
	//pngwriter* dest = new pngwriter(width, height, .5, "csmirror.png");
	//for (int i = 1; i <= width; i++) {
	//	for (int j = 1; j <= height; j++) {
	//		red = source->read(i, j, 1);
	//		green = source->read(i, j, 2);
	//		blue = source->read(i, j, 3);
	//		// This creates a mirror image of the original image
	//		dest->plot(width - i + 1, j, red, green, blue);
	//	}
	//}
	//source->close();
	//dest->close();
	// ==================== 2 ====================

	//int height, width, red, blue, green, gray;
	//pngwriter* source = new pngwriter();
	//source->readfromfile("sc.png");
	//width = source->getwidth();
	//height = source->getheight();
	//pngwriter* dest = new pngwriter(width, height, .5, "csgray.png");
	//for (int i = 1; i <= width; i++) {
	//	for (int j = 1; j <= height; j++) {
	//		red = source->read(i, j, 1);
	//		green = source->read(i, j, 2);
	//		blue = source->read(i, j, 3);
	//		//gray = (red + green + blue) / 3;
	//		//OR
	//		gray = source->avg
	//		
	//		// This creates a gray scale image of the original image
	//		dest->plot(i, j, gray, gray, gray);
	//	}
	//}
	//source->close();
	//dest->close();
	return 0;
}*/